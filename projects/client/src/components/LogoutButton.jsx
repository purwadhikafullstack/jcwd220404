import {
  Button,
  ButtonGroup,
  Popover,
  PopoverArrow,
  PopoverBody,
  PopoverContent,
  PopoverFooter,
  useDisclosure,
} from "@chakra-ui/react";

export const LogoutButton = () => {
  const { isOpen, onOpen, onClose, onToggle } = useDisclosure();

  return (
    <div>
      
    </div>
  );
};
